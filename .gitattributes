# Track large model and binary files with Git LFS
# Patterns below will cause matching files to be stored using git-lfs

# Track common deep learning model binaries and tokenizers
models/** filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.bin filter=lfs diff=lfs merge=lfs -text
*.h5 filter=lfs diff=lfs merge=lfs -text
*.model filter=lfs diff=lfs merge=lfs -text

# Track large embeddings / vector stores
*.npy filter=lfs diff=lfs merge=lfs -text
*.npz filter=lfs diff=lfs merge=lfs -text

# Large tokenizer models / weights
tokenizer.json filter=lfs diff=lfs merge=lfs -text
vocab.txt filter=lfs diff=lfs merge=lfs -text
tokenizer_config.json filter=lfs diff=lfs merge=lfs -text

# If you keep dataset files in the repo
data/** filter=lfs diff=lfs merge=lfs -text

# Add a note for maintainers
# To start using LFS locally: `git lfs install` then `git add .gitattributes` and `git commit -m "Add .gitattributes for LFS"`.
